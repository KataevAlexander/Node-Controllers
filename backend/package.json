{
  "name": "backend",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "fs.extra": "^1.2.1"
  },
  "dependencies": {
    "express": "^4.10.6",
    "swig": "^1.4.2"
  }
}
